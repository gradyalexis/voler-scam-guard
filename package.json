{
  "name": "voler-scam-guard",
  "version": "1.0.0",
  "private": true,
  "description": "Discord anti-scam bot",
  "scripts": {
    "typecheck": "npm --prefix bot run typecheck",
    "backup": "./scripts/backup.sh",
    "up": "docker compose up -d --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f bot"
  }
}
